
	// Displacement doesnt work right yet.
	//if (snow_enabled && snow_displacement_enabled && snow_angle_to_up < (max_snow_angle - 10.0)) {
		//float blend_ratio = (1.0 - (snow_angle_to_up / max_snow_angle));
		//if (blend_ratio > 0.95) {
			//VERTEX += NORMAL * (texture(snow_albedo_texture, snow_uv).r * snow_depth * (1.0 - (snow_angle_to_up / max_snow_angle)) - 2.0);
		//}
	//}